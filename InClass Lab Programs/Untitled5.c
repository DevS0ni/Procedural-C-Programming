#include<stdio.h>
int main()
{
	int i;
	printf("Enter the value of i:");
	scanf("%d",&i);
	if(i>0)
	{
		printf("i is positive");
		}
		else
		{
		printf("i is negative");
}
	return 0;
}
